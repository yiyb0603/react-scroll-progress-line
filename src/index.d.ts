import React from 'react';
import { ScrollProgressLineProps } from '.';

declare const ScrollProgressLine: React.MemoExoticComponent<({ backgroundColor, progressColor, customStyle, disableClick, }: ScrollProgressLineProps) => JSX.Element>;
export default ScrollProgressLine;